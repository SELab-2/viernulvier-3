import { type Config } from "prettier";

const config: Config = {
  printWidth: 100,
  trailingComma: "es5",
  semi: true,
  tabWidth: 2,
  useTabs: false,
  endOfLine: "lf",
  arrowParens: "always",
};

export default config;
